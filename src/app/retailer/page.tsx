import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Retailer Dashboard",
  description: "This is our CTD Project Retailer Dashboard page",
};

const RetailerDashboard = () => {
  return <main></main>;
};

export default RetailerDashboard;
