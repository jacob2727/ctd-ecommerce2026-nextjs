import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Dashboard",
  description: "This is our CTD Project Dashboard page",
};

const Dashboard = () => {
  return <main></main>;
};

export default Dashboard;
